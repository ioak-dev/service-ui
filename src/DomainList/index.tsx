import React, { useState, useEffect } from "react";
import { Input, SvgIcon, Button, ButtonVariantType } from 'basicui';

export type DomainListProps = {
};

/**
 * Component to render search bar with advanced options.
 */
const DomainList = (props: DomainListProps) => {

    return (
        <div className="serviceui-domainlist">Domain list
        </div>
    );
};

export default DomainList;
